Serveur:

- Mettre a jour le score (wardog) / OK

- Fonction pour chaque niveau avec pop des mobs (polo) 

- Avant chaque level (lancement d´un boss) (polo)

- Modifier la boucle pour que le serveur sature pas

- Corriger les segfaults (wardog)

- IA pour chaque mobs (polo)

- Rajouter les libs (ludo)

- Refaire les Makefile (wardog) / OK

Client:

- A chaque level changement du background

- Implementer les sons au niveau des tirs
